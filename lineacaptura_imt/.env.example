APP_NAME="Sistema de Línea de Captura IMT"
APP_ENV=local
APP_KEY=base64:BAOhAYsAaoSd08I01dQDxxwBOfjfgYRCgjYGTiZ2qKI=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=es
APP_FALLBACK_LOCALE=es
APP_FAKER_LOCALE=es_MX

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# ==========================================================
#  CONFIGURACIÓN DE LA API DEL SAT
#  ¡AQUÍ DEBES COLOCAR LA URL Y CREDENCIALES CUANDO LAS TENGAS!
# ==========================================================

# URL de la API del SAT para validación de líneas de captura
SAT_API_URL=https://sat.azure-api.net/dpas/manual/paths/invoke

# Token de autenticación para la API del SAT (si es necesario)
# SAT_API_TOKEN=tu_token_aqui

# Clave de API para el SAT (si es necesario)
# SAT_API_KEY=tu_api_key_aqui

# Clave de suscripción de Azure API Management (si aplica)
# Usada por el header 'Ocp-Apim-Subscription-Key'
SAT_SUBSCRIPTION_KEY=

# Timeout en segundos para las peticiones a la API del SAT
SAT_API_TIMEOUT=30

# ==============================
# SAT mTLS Client Certificate
# ==============================
# Ruta al certificado de cliente (PEM o P12/PFX)
SAT_CLIENT_CERT_PATH=
# Tipo de certificado: PEM o P12
SAT_CLIENT_CERT_TYPE=PEM
# Contraseña del certificado si aplica (P12/PFX)
SAT_CLIENT_CERT_PASSWORD=
# Ruta a la llave privada si el cert es PEM separado
SAT_CLIENT_KEY_PATH=
# Contraseña de la llave privada si aplica
SAT_CLIENT_KEY_PASSWORD=
# Ruta al certificado de CA (cadena) proporcionado por SAT
SAT_CA_CERT_PATH=
# Verificar TLS (true recomendado; false solo para pruebas)
SAT_TLS_VERIFY=true

# ==============================
# SAT APIM / HTTP Client Options
# ==============================
# Idioma preferido en las respuestas
SAT_ACCEPT_LANGUAGE=es-MX
# Habilitar trazas en Azure APIM
SAT_APIM_TRACE=false
# Forzar HTTP/2 si disponible
SAT_HTTP2=false
# Timeout de conexión (segundos)
SAT_API_CONNECT_TIMEOUT=10
# Verbose de cURL (solo para debugging)
SAT_CURL_VERBOSE=false
# Enviar x-correlation-id en cada petición
SAT_SEND_CORRELATION_ID=true
# Si necesitas fijar un correlation-id específico (opcional)
SAT_CORRELATION_ID=

# =============================
# JWT para autorización SAT (HS256)
# =============================
# Habilita generación automática de JWT HS256 si no se define SAT_API_TOKEN
SAT_JWT_ENABLE=false
# Clave secreta proporcionada por SAT para firmar HS256
SAT_JWT_SECRET=
# Claims opcionales para el JWT
SAT_JWT_AUD=www.sat.gob.mx
SAT_JWT_ISS=
SAT_JWT_SUB=
# Tiempo de expiración del token en segundos (por defecto 5 minutos)
SAT_JWT_EXP_SECONDS=300
# Esquema del header Authorization: "Bearer" o "Plain"
SAT_AUTH_SCHEME=Bearer
